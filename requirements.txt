numpy
streamlit
yt-dlp
ultralytics
# Note: `ultralytics` may install `opencv-python` (GUI build) which fails on Streamlit Cloud
# with `libGL.so.1` errors. `postBuild` force-uninstalls it and reinstalls headless.
opencv-python-headless==4.11.0.86
shapely>=2.0.0,<3
 
# Optional (only needed for "Webcam (streamlit-webrtc)" mode):
# streamlit-webrtc
# av
